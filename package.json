{
  "name": "@jurca/szn-options",
  "version": "0.0.9",
  "description": "Accessible HTML selectbox options list with customizable UI. Based on web components and easy to integrate with various frameworks like React or Angular.",
  "main": "szn-options.js",
  "scripts": {
    "test": "eslint --max-warnings=0 *.js && jest",
    "build": "gulp",
    "prepublishOnly": "echo Run \\'npm run publish-package\\' instead && exit 1",
    "publish-package": "git push && git push --tags && npm run build && npm publish --ignore-scripts dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jurca/szn-options.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "keywords": [
    "html",
    "web",
    "component",
    "components",
    "select",
    "selectbox",
    "option",
    "szn",
    "seznam",
    "seznam.cz",
    "accessible",
    "accessibility",
    "a11y"
  ],
  "author": "Martin Jurča <martin.jurca@firma.seznam.cz>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jurca/szn-options/issues"
  },
  "homepage": "https://github.com/jurca/szn-options#readme",
  "peerDependencies": {
    "@jurca/szn-elements": "^1.0.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "del": "^3.0.0",
    "eslint": "^4.9.0",
    "eslint-config-fbjs-opensource": "^1.0.0",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-babel": "^7.0.0",
    "gulp-postcss": "^7.0.0",
    "gulp-rename": "^1.2.2",
    "jest": "^21.2.1",
    "postcss-calc": "^6.0.1",
    "postcss-css-variables": "^0.8.0",
    "postcss-import": "^11.0.0"
  }
}
